{
  "name": "@fishy/core",
  "version": "0.0.1",
  "private": true,
  "description": "Core Holochain conductor functionality for Fishy",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fishy/shared": "*",
    "@msgpack/msgpack": "^3.0.0",
    "libsodium-wrappers": "^0.7.13"
  },
  "devDependencies": {
    "@types/libsodium-wrappers": "^0.7.14"
  }
}
