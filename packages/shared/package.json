{
  "name": "@hwc/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types and utilities for Holo Web Conductor",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  }
}
