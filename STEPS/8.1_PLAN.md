# Step 8.1: DhtOp Generation in Extension

## Goal

Convert source chain records to DhtOps after each commit, enabling data to be published to the DHT.

## DhtOp Types

Each action type generates specific DhtOps:

| Action Type | DhtOps Generated |
|------------|------------------|
| Create | StoreRecord, RegisterAgentActivity, StoreEntry |
| Update | StoreRecord, RegisterAgentActivity, StoreEntry, RegisterUpdatedContent, RegisterUpdatedRecord |
| Delete | StoreRecord, RegisterAgentActivity, RegisterDeletedBy, RegisterDeletedEntryAction |
| CreateLink | StoreRecord, RegisterAgentActivity, RegisterAddLink |
| DeleteLink | StoreRecord, RegisterAgentActivity, RegisterRemoveLink |

## OpBasis (DHT Routing)

Each DhtOp has a basis that determines which DHT authorities receive it:

- **StoreRecord**: ActionHash (action authority)
- **StoreEntry**: EntryHash (entry authority)
- **RegisterAgentActivity**: AgentPubKey (chain authority)
- **RegisterUpdatedContent**: Original EntryHash
- **RegisterUpdatedRecord**: Original ActionHash
- **RegisterDeletedBy**: Deleted ActionHash
- **RegisterDeletedEntryAction**: Deleted EntryHash
- **RegisterAddLink**: Base address
- **RegisterRemoveLink**: Base address

---

## Implementation Tasks

1. Define DhtOp TypeScript types matching Holochain's types
2. Implement `produce_ops_from_record()` function
3. Implement OpBasis computation for each op type
4. Store pending DhtOps in IndexedDB

---

## Files to Create/Modify

**New files:**
- `packages/core/src/dht/dht-op-types.ts` - Type definitions
- `packages/core/src/dht/produce-ops.ts` - Op generation logic

**Modified files:**
- `packages/core/src/storage/types.ts` - Add PendingPublish type

---

## Testing Strategy

```typescript
test('Create action produces StoreRecord, RegisterAgentActivity, StoreEntry ops')
test('Update action produces 5 ops including RegisterUpdatedContent')
test('Delete action produces RegisterDeletedBy and RegisterDeletedEntryAction')
test('CreateLink produces RegisterAddLink op')
test('OpBasis is correct for each op type')
```

---

## Dependencies

- Step 8.0 (Hash Computation) must be complete
